# Pokedex Frontend

React/Vite frontend for the Pokedex Favorites App.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run lint
```

## Configuration

Create `.env` from `.env.example` when the backend is not running on the default local URL.

```env
VITE_API_BASE_URL=http://localhost:3001
```
